# Runtime dependencies for Railway / Railpack.
# Railpack installs from requirements.txt reliably (it does not auto-install a
# PEP 621 setuptools pyproject's [project.dependencies]). Keep this in sync with
# pyproject.toml [project.dependencies] — that file stays the source of truth for
# local dev (`pip install -e ".[dev]"`) + tooling config (ruff).
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
gspread>=6.0
google-auth>=2.28
python-multipart>=0.0.9
posthog>=3.5
# Supabase Storage — private bucket for WZ goods-receipt photos (GR-01); pulls httpx.
supabase>=2.10,<3
