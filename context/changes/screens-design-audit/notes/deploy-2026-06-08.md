# Deploy record — 2026-06-08 (out-of-band, owner-requested)

Not part of the screens-design-audit change. The owner asked to commit/push/deploy
the accumulated work so they have a working test link before plan-review.

## What shipped
- **Pushed** 21 commits → `origin/main` (`8a55cd4`): email-fix, inventory-category-sections,
  frontend-lint-debt, order-prefill (S-07), channel-aware-dispatch (S-04), queue-filters (S-05),
  + this change's planning docs.
- **Backend (droplet `46.101.213.61`, systemd `jarvis-supply-os`)**: rsynced new `supply-os-v1/`
  (sa.json excluded — secret), `pip install -e .`, restart. Now serves inventory routes.
  - Rollback: `/opt/pitabros/supply-os/app.bak-20260608-091802` (restore + `systemctl restart`).
- **Frontend (Vercel `pita-supply-os`)**: `vercel --prod` → `dpl_7TC6P5EzLEi9fSxQTVpQ6on3NFk2`,
  aliased to `pita-supply-os.vercel.app`. Rollback: promote previous deployment in Vercel.

## Verified (no secrets, no orders)
- Backend public: `/health`=200, `/api/captain/inventory/products`(no token)=401 (route exists).
- Vercel domain: root=200; `/api/*` proxy → droplet → 401 (auth enforced end-to-end).

## Live links
- Captain: https://pita-supply-os.vercel.app/captain-v2
- Manager: https://pita-supply-os.vercel.app/manager
- Auth: **dev tokens reset** in droplet `.env` (Captain `WOLA:` + Manager) to known test codes,
  service restarted, verified 200/200/401 via the Vercel proxy. Codes kept out of the repo —
  they live in chat / the owner's password manager. `.env` backup: `.env.bak-<ts>` on the droplet.
  Rotate before wider rollout (Open Question 4).

## Safety
- Backend writes to the real Google Sheet; email dispatch sends a real Gmail draft.
  Test with submit-and-back-out / safe data — never place a real supplier order.
